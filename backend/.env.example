# Apollo.io API Key (Required for decision maker & corporate firmographics)
APOLLO_API_KEY=your_apollo_api_key_here

# Database URL (Defaults to SQLite leads.db if not provided)
DATABASE_URL=sqlite:///./leads.db
